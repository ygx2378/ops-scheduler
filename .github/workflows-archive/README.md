# Archived workflows

These workflows are retained for historical reference only. GitHub Actions does not execute workflow files outside `.github/workflows`.

The active production flow uses the local SQLite/R2 refresh, the hot App Store monitor, the verified-price publisher, and the purchase-channel refresh.
